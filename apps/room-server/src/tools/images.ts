/**
 * Step 4 stub — image generation and editing.
 *
 * Implement `generate_image` and `edit_image` here. See BUILD_PLAN.md step 4
 * for the full prompt.
 *
 * Provider suggestion: fal.ai
 *   - FLUX.1 [schnell] for generation
 *   - FLUX.1 Kontext for editing
 *
 * After upload to Cloudflare R2, return the public URL. The agent loop will
 * broadcast the tool_result containing the URL, and the web UI will render
 * it as an inline image.
 */

export {};
