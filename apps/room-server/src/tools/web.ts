/**
 * Step 5 stub — web_search and browse via Browserbase.
 *
 * See BUILD_PLAN.md step 5 for the implementation prompt. Key constraints:
 *   - Cache search results in Upstash Redis for 1 hour
 *   - Return tool_result with is_error:true on session failure
 *   - browse() should return readable text, not raw HTML
 */

export {};
